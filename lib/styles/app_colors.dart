// styles/app_colors.dart
import 'dart:ui';

class AppColors {
  // Colores principales
  static const Color primaryColor = Color(0xFF058ED9);     // Azul principal
  static const Color secondaryColor = Color(0xFF483D3F);   // Marrón oscuro
  static const Color backgroundColor = Color(0xFFF4EBD9);  // Beige claro
  static const Color neutralColor = Color(0xFFA39A92);     // Gris claro
  
  // Colores de estado
  static const Color successColor = Color(0xFF4CAF50);     // Verde éxito
  static const Color errorColor = Color(0xFFE53935);       // Rojo error
  static const Color warningColor = Color(0xFFFF9800);     // Naranja warning
  static const Color infoColor = Color(0xFF2196F3);        // Azul info
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);      // Texto principal
  static const Color textSecondary = Color(0xFF757575);    // Texto secundario
  static const Color textHint = Color(0xFFBDBDBD);         // Texto hint
  static const Color textLight = Color(0xFFFFFFFF);        // Texto claro
  
  // Colores de precio
  static const Color priceColor = Color(0xFF2E7D32);       // Precio normal
  static const Color discountPrice = Color(0xFFE53935);    // Precio con descuento
  
  // Colores de fondo adicionales
  static const Color cardBackground = Color(0xFFFFFFFF);   // Fondo de tarjetas
  static const Color surfaceColor = Color(0xFFF5F5F5);     // Color de superficie
  
  // Colores de borde
  static const Color borderColor = Color(0xFFE0E0E0);      // Color de borde
  static const Color dividerColor = Color(0xFFEEEEEE);     // Color divisor
}